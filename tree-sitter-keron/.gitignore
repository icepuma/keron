# Build deps; rebuildable via `npm install`.
/node_modules/
/build/

# Language-binding wrappers we don't ship; tree-sitter regenerates
# them on demand from `grammar.js`.
/bindings/

# Generated parser.c IS committed: Zed and other consumers expect to
# find `src/parser.c` after cloning the grammar repo, since they do
# not run `tree-sitter generate` themselves.
