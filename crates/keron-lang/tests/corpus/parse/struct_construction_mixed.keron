# Field shorthand mirrors the pattern shorthand: `x` binds the
# same-named variable, exactly the inverse of `Point { x }` in a
# match arm.
struct Point { x: Int, y: Int }

val x: Int = 0

val p: Point = Point { x, y: 9 }
