# Required fields must precede defaulted ones — same rule fn
# parameters follow. Otherwise positional resolution becomes
# ambiguous about which positional fills which field.
struct Bad {
  a: Int = 1,
  b: Int,
}
