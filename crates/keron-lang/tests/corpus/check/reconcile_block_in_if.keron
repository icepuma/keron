from "std:fs" use symlink

val use_zsh: Boolean = true
val zshrc: Symlink = symlink(from = "df/zshrc", to = "~/.zshrc")
val bashrc: Symlink = symlink(from = "df/bashrc", to = "~/.bashrc")

if use_zsh {
  reconcile {
    zshrc;
    bashrc -> zshrc
  }
}