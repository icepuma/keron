type Kind = "link" | "tmpl"

# Arms returning different resource singletons (`Symlink`, `Template`)
# lift to `Resource`, mirroring the list-inference rule.
fn make(k: Kind): Resource {
  match k {
    "link" => symlink(from = "a", to = "b"),
    "tmpl" => template(path = "p", source = "t.tpl", vars = {}),
  }
}
