# `parse_int` / `parse_double` are strict (no whitespace, no
# fallthrough to NaN / inf for double) and return nullable types so
# `??` is the natural recovery path — mirrors `env(...)` shape.
val n: Int = parse_int("42") ?? 0

val d: Double = parse_double("3.14") ?? 0.0
